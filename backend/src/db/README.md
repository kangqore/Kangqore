# Database Module

This folder centralizes database access:

- `prisma.ts` - Prisma client instance
- `queries/` - Common database queries (optional)

## Usage

```typescript
import { prisma } from './db';
```
