// Design system tokens — re-exported from @os/tokens for unified access
export { surface, border, brand, text, status, radius, shadow, tokens as colors } from '@os/tokens'
export { font, fontSize, fontWeight } from '@os/tokens'
export { space as spacing } from '@os/tokens'
